services:
  healsync-backend:
    container_name: healsync-backend
    env_file:
      - .env
    build:
      context: .
      dockerfile: ./Dockerfile
    expose:
      - '${SERVICE_PORT}'
    ports:
      - '${HOST_PORT}:${SERVICE_PORT}'
    networks:
      healsync:
        aliases:
          - healsync-backend
  # healsync-db:
  #   image: postgres:16
  #   container_name: healsync-db
  #   env_file:
  #     - .env
  #   ports:
  #     - '5432:5432'
  #   volumes:
  #     - /root/healsync-backend/volumes/healsync-db-data:/var/lib/postgresql/data
  #   networks:
  #     - healsync
networks:
  healsync:
    external: true

# volumes:
#   healsync-db-data:
#     driver: local